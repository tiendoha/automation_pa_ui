# PostgreSQL connection used by migration and runtime persistence
DATABASE_URL=
# Optional. Without both values, notification remains dry-run only.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# Comma-separated query parameters that may be ignored when fingerprinting.
FINGERPRINT_IGNORE_QUERY_PARAMS=
# Directory for local screenshot and JSON evidence.
EVIDENCE_ROOT=./evidence
